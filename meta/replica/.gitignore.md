# `.gitignore`

Source: `.gitignore`
Ignores build/test outputs, including the top-level `.make` scratch tree and
direct local-stage outputs such as `.o/`, `test/.o/`, and `test/.exe/`.
It also ignores Python bytecode caches, local temporaries, and third-party
checkout state.
