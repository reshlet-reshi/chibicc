# `.gitignore`

Source: `.gitignore`
Ignores build/test outputs, including the top-level `.make` scratch tree,
direct local-stage object files, `test/*.exe`, Python bytecode caches, local
temporaries, and third-party checkout state.
